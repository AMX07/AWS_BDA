[project]
name = "aws-bda-pipeline"
version = "0.1.0"
description = "Intelligent document processing pipeline using AWS Bedrock Data Automation"
authors = [{ name = "AutoGenerated", email = "autogen@example.com" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.34.140",
    "botocore>=1.34.140"
]

[project.scripts]
run-bda-pipeline = "bda_pipeline.main:main"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
