# AWS Credentials (Optional - you can also use AWS CLI configuration)
# If you prefer to use environment variables instead of AWS CLI profiles:

# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# AWS_SESSION_TOKEN=your_session_token_here (if using temporary credentials)
# AWS_DEFAULT_REGION=us-east-1

# Note: It's recommended to use AWS CLI profiles or IAM roles instead of
# hardcoding credentials in environment variables for better security
